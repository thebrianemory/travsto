<div class="container">
  <div class="col-md-7">
    <h2><%= @user.username + "'s trips" %></h2>
    <% @my_trips.each do |trip| %>
      <div class="col-md-10">
        <h4><%= link_to trip.title, trip_path(trip) %></h4>
      </div>
    <% end %>
  </div>
</div>
